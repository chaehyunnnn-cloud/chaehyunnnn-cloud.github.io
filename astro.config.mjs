import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // GitHub Pages 사용자 사이트(루트 서빙) — base 불필요, /images 등 절대경로 그대로 동작
  site: 'https://chaehyunnnn-cloud.github.io',
});
